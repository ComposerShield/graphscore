# SPDX-License-Identifier: Apache-2.0
#
# Package config for GraphScore Runtime. Generated by
# configure_package_config_file(); see cmake/RuntimePackage.cmake.
#
#   find_package(GraphScoreRuntime REQUIRED)
#   target_link_libraries(my_app PRIVATE graphscore::runtime)
#
# The package has no dependencies to find: the runtime closure is
# self-contained by construction (ADR 0003 §3.1), which is exactly why a
# consumer needs nothing but this package.

@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/GraphScoreRuntimeTargets.cmake")

check_required_components(GraphScoreRuntime)
