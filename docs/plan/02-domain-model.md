# Milestone 02: Domain And Command Model

## Goal

Implement the toolkit-independent source of truth for notation, graph structure, identities, editing commands, and validation.

## Dependencies

- [x] Milestone 01 foundation.
- [x] Milestone 00 architecture decisions.

## Deliverables

### Identity and value types

- [x] Strong UUID wrappers for projects, tracks, staves, nodes, connectors, events, and notation entities.
- [x] Stable project-order indexes generated separately from persistent identity.
- [x] Exact rational musical positions and durations; avoid floating-point equality for score structure.
- [x] Validated MIDI pitch, channel, velocity, accidental, clef, key signature, time signature, voice, and tempo value types.
- [x] Explicit conversions at API/serialization boundaries and no implicit unit mixing.

### Project and track model

- [x] Project metadata, designated start node, project tempo/dynamic defaults, and event registry.
- [x] Up to 64 globally active track definitions with fixed one-or-more-staff layouts and MIDI channels.
- [x] Standard pitched and grand-staff configurations.
- [x] Archived removed tracks retain recoverable notation but are excluded from active playback/export.
- [x] Adding a track creates an empty aligned track lane in every node.

### Node timeline

- [x] A common ordered measure map shared by every active track in a node.
- [x] Per-measure time and standard key signatures.
- [x] Clef changes at exact musical positions.
- [x] At least one complete main-region measure plus an optional explicit final pickdown duration greater than zero and shorter than one measure under the boundary's active meter.
- [x] Tempo curves cover the main region and pickdown; crossing notes/ties identify their main and tail ownership at the boundary.
- [x] No opening partial measure in the `0.1.0` model.
- [x] A node name, stable UUID, custom color, freeform notes, and graph position.

### Notation model

- [x] Four explicit voices per staff, each rhythmically complete through normalized rests.
- [x] Notes, chords, rests, ties, whole through sixty-fourth durations, single/double dots, and arbitrary single-level `N:M` tuplets.
- [x] Grace-note groups attached to principal events with playback time stolen from the preceding note.
- [x] Dynamics, hairpins, slurs, accent, marcato, staccato, staccatissimo, tenuto, beam overrides, stem overrides, and pedal spans.
- [x] Individual note spelling through double-flat and double-sharp.
- [x] Referential validation for ties, slurs, hairpins, tuplets, beams, and spans after edits.

### Graph model

- [x] Any number of named stable input and output connectors per node.
- [x] Zero or one destination edge per output while editing; enabled exportable outputs require exactly one.
- [x] Sequential and vertical connector types with type-appropriate trigger, priority, and weight fields; queue policy/capacity is stored once per node/event listener.
- [x] Registered event UUID/name lookup with unique case-sensitive UTF-8 names.
- [x] Normative persistent event state machine: bounded occurrence storage per event, arrival sequence, per-node/event first/latest/FIFO interpretation, cross-event connector-priority arbitration, stable tie-breaking, consumption/discard rules, pause behavior, and clearing on stop/reset/node-play.
- [x] Orthogonal route geometry stored as automatic or user-customized interior segments, independent of runtime semantics.

### Adaptive playback semantics

- [x] Exact definitions for sequential precedence, 100-percent random groups, zero-weight ineligibility, deterministic PRNG reset, vertical compatibility/mapping, sample-local vertical triggers, and one vertical jump per sample offset.
- [x] Pickdown meter/tempo coordinates, crossing-note ownership, MIDI-only behavior, minimum main-region duration, and requirements for proving finite overlap through cycles.
- [x] Same-pitch newest-owner retrigger behavior and logical-OR CC64 ownership across active main material and tails.
- [x] Lifecycle rules remove note and pedal ownership on vertical jump, stop, reset, node play, panic, and snapshot retirement; pause retains ownership.
- [x] A toolkit-independent writer audition model describes one opaque instrument slot, zero or more opaque effect slots, plugin identity/state blobs, bypass/order, and writer-only mix values without depending on the VST3 SDK.

### Normative playback specification

- [x] Specify cubic smooth-tempo curve equations, legal control handles, deterministic integration/inversion tolerances, and integer sample rounding at boundaries.
- [x] Enumerate articulation mappings and precedence with slurs, ties, dynamics, and hairpins.
- [x] Specify grace-group steal fraction/limits, multi-note division, behavior without a preceding sounded note, and interaction with articulations.
- [x] Define simultaneous MIDI ordering and exact note/CC64 ownership transitions independently of implementation.

### Command and selection model

- [x] **Phase 8a — foundational command protocol, standalone history, and atomic transaction grouping (done, committed).** Delivered the non-throwing `Command` ABC with `execute`/`undo`/`redo` all `noexcept` and returning `Result`; `CommandHistory` (standalone undo/redo stacks with allocation-safe pre-reserve before any model mutation); `CommandTransaction` (ordered-child atomic grouping with best-effort rollback on any child failure, explicit `kCommandFaulted`/`kTransactionRollbackFailed` terminal states, and exact restoration semantics on undo/redo compensation failure); three stable-ID proving commands (`SetNodeNameCommand`, `SetTrackNameCommand`, `SetProjectTempoCommand`) exercising the full three-phase lifecycle against real domain types. `Result` and `ResultCode` extended with two new terminal codes. `Node::set_name` and `Track::set_name` tightened to `noexcept`.
- [x] **Phase 8b — ten reversible non-structural metadata commands (done, committed).** Delivered the remaining single-field project, track, and node commands against stable `NodeId`/`TrackId` lookup with identical-noexcept-snapshot-semantics as 8a. Project commands: `SetProjectNameCommand` (UTF-8 `std::string`, allocation-safe, empty/long/multibyte round-trips), `SetStartNodeCommand` (optional set or clear with `kInvalidArgument` on unowned `NodeId`, re-validated on each phase), `SetProjectDynamicCommand` (all eight `Dynamic` values round-trip). Track audition-mix commands: `SetTrackGainCommand`, `SetTrackPanCommand`, `SetTrackMuteCommand`, `SetTrackSoloCommand` — each rejects missing and archived `TrackId`s without mutation; gain/pan store every IEEE 754 `float` bit-pattern exactly (NaN/inf/subnormal/+0/−0 included, legal-range validation deferred to M08). Node metadata commands: `SetNodeColorCommand` (packed `uint32_t` RGBA, all bytes exact), `SetNodeNotesCommand` (freeform `std::string`, empty/long/UTF-8 round-trips), `SetNodePositionCommand` (`GraphPosition` double-pair, every IEEE 754 bit-pattern stored exactly, unrelated fields unchanged). Identical-command-stream deterministic replay evidence: all ten commands snapshot the old value on first successful execute; restore it on undo; re-apply the new value on redo. The eight original Phase 8 deliverable boxes below remain unchecked: exhaustive edit commands, selection, clipboard, clipping/remapping, and measure operations — plugin-chain, structural, graph, timeline, and notation commands are still outstanding.
- [x] **Phase 8c — fourteen reversible structural/config commands wrapping only existing domain API (done, committed).** The reversible-today subset: every graph/connector/track edit that is exactly undo/redo-able with stable ids using the mutators that already exist, no new domain API. **8c-i** (nine commands): `ArchiveTrackCommand`/`RestoreTrackCommand` (inverse pair, restore in place), `SetOutputTypeCommand` (restores the shared `EventListener::bound_type()`), `SetListenerPolicyCommand` (policy and capacity), `SetOutputPriorityCommand`, `SetOutputWeightCommand`, `SetOutputExportEnabledCommand`, `SetInputConnectorNameCommand`, `SetOutputConnectorNameCommand`. **8c-ii** (five commands with reversibility subtleties): `ConnectCommand` and `DisconnectCommand` (each snapshots and restores the source output's `RouteGeometry`, because `disconnect` resets a customized route to automatic), `BindOutputEventCommand` (snapshots the old event's listener policy/capacity so undo recreates a listener destroyed when the last bound output is rebound away — resolves the `TODO(Phase 8)` at `node.hpp:145`), `SetCustomRouteCommand`, `ResetRouteCommand`. Each rejects a missing id without mutation, propagates the wrapped call's failure while leaving its state and the model unchanged, snapshots once on first successful execute. 112 new command test cases (896 total; `CommandTest.*` 105→222), debug + ASan/UBSan clean, all seven architecture audits pass. **Scope decision (Adam):** the add/remove of nodes, tracks, connectors, and events is NOT in 8c because it cannot be made exactly reversible ("undo/redo exactly / stable intended IDs") with today's domain API — there is no `remove_node`, no hard track removal, connectors mint fresh ids with no restore-with-id, and no event register-with-id. Those commands are deferred to Phase 8d, which must first add the required domain restore-with-id/removal API. The five boxes below (add/remove structural, selection, clipboard, clip/reconnect rules, node copy/paste, measure ops) remain unchecked.
- [x] **Phase 8d — reversible add/remove of graph entities via new restore-with-id domain API (done, committed, split 8d-i..iv).** The structural add/remove commands deferred from 8c because they require new domain API. Locked mechanism: snapshot + restore-with-id, every new primitive guarding id-uniqueness. **8d-i** connectors (`Node::restore_input`/`restore_output`; `AddInputConnector`, `AddOutputConnector`, `RemoveOutputConnector` restoring a destroyed listener, `RemoveInputConnector` restoring every cross-node inbound edge + route). **8d-ii** events (`EventRegistry::add_event_with_id`; `RegisterEventCommand`, `RemoveEventCommand` restoring the definition + every binding + each node's listener policy/capacity). **8d-iii** tracks (`Project::add_track_with_id` + undo-only `hard_remove_track` + `Node::remove_lane`; `AddTrackCommand` only — archive remains the user-facing removal, so no `RemoveTrackCommand`). **8d-iv** nodes (`Project::add_node_with_id`/`remove_node`/`restore_node`; `AddNodeCommand`, `RemoveNodeCommand` with full Node-aggregate snapshot + inbound-edge/route/start-node cascade, self-loops restored via the Node value, resolving the Phase-2 dangling-start-node deferral). Plus a family-wide `bind_output_event` allocation-safety hardening. 976 tests (`CommandTest.*` 222→280). All five gates (clang-tidy now enforced at commit) green each increment. **This box (below) stays unchecked:** notation and tempo edit commands are still outstanding (they need fine-grained `VoiceContent`/`TempoLane` mutators), so "every graph, notation, tempo, track, and metadata edit" is not yet complete — those land with 8e..f.
- [x] **Phase 8e — reversible notation and tempo edit commands (done, committed, split 8e-i..iii).** The editing core, and the prerequisite for the clipboard cluster. Reversibility throughout is whole-container snapshot (copy the `VoiceContent`/`TrackLane`/tempo-point vector before the edit, restore on undo) — the same robust pattern as 8d-iv's whole-`Node` snapshot, avoiding fragile per-edit inverses. **8e-i** core voice-event edits: new positional `VoiceContent`/`TrackLane` mutators (insert/remove/replace with automatic rest normalization, which were append/clear-only before) plus `SetEventCommand` (note↔rest↔chord conversion, duration change, chord-build, with rest-consuming duration expansion), `ConvertEventToRestCommand`, `SetTieCommand`; every edit leaves the voice rhythmically complete via the existing `normalize`/`check_complete` pipeline. **8e-ii** marking edits: six `Add*`/`Remove*` command pairs for dynamics, hairpins, slurs, beam overrides, grace groups, and pedal spans via new marking-removal mutators and shared `marking_command_helpers`; hardened cross-kind notation ID uniqueness so a single-id lookup cannot resolve across Dynamic/Hairpin/Slur boundaries; sounding-slur endpoint validation (grace notes cannot be slur endpoints); transactional `add_pedal_span`/`remove_pedal_span` with guard-before-mutate discipline. **8e-iii** tempo-point edits: `AddTempoPointCommand`, `RemoveTempoPointCommand`, `MoveTempoPointCommand`, `SetTempoPointCommand`, each rebuilding the lane's point vector and re-validating through `NodeTimeline::set_tempo` (atomic on failure via `TempoLane::create`). One new domain method, `NodeTimeline::clear_tempo() noexcept` — required because the timeline previously had only `set_tempo`, leaving the create-the-first-lane edit with no inverse. Both snapshots are `std::optional<std::vector<TempoPoint>>` where an empty optional means "this node had no tempo lane", which is what makes add-first-point (undo removes the lane) and remove-last-point (execute removes the lane) exactly reversible in both directions. Lane invariants are propagated rather than reimplemented: adding a first point at a nonzero position, and removing the point at position 0 while later points remain, both fail with the model unchanged. 1211 tests (`CommandTest.*` 280→506); all five gates plus ASan/UBSan green. **The box below stays unchecked:** node-timeline edits still have no commands — clef changes (`ClefLane`), pickdown set/clear, and per-measure time/key-signature changes — so "every notation edit" is not yet complete. **Scope decision (Adam):** those three families fold into the measure-operations phase (8h) rather than becoming a separate 8e sub-increment, because per-measure signature commands depend on the `MeasureMap` mutators 8h introduces (`MeasureMap` has none today) and the clef/pickdown commands are cheap to carry alongside them. Line 79 is therefore an **8h** deliverable.
- [x] **Phase 8f-ii — selection representation (done, committed).** Delivered the `Selection` variant: seven homogeneous non-empty deduplicated set arms (notehead, chord, full-measure, arbitrary range, node, connector, insertion caret) each carrying per-item scope; separate deterministic `validate_selection` free function enforcing archived/wrong/missing scope, notehead/chord kind, main-only measure ordinals, raw range classification including pickdown, local connector ownership, and caret event-boundary plus `TrackLane::total_length` extent; `VoiceContent::position_of_event` covering ChordNote/GraceNote principal chains with dangling/cycle guards; `TrackLane::total_length` deterministic max across staves/voices. 1335 tests (debug + ASan/UBSan). `route-segment` selection deferred to M06; `staff-focus` dropped. 8g clipboard and 8h measure ops remain outstanding.
- [x] **Phase 8g-i — notation clipboard fragment representation and copy extraction (done, committed).** The toolkit-independent clipboard type and the pure copy query, the prerequisite for the cut/paste command cluster. `NotationFragment` is a validated value type (`static create`, private constructor) holding a span length, per-track stave shapes, and `FragmentVoicePart`s keyed by `(track_ordinal, stave_ordinal, voice)` — ordinals resolved through `Track::index()` and `Track::layout().staves()`, never the documented-non-deterministic `TrackLane::stave_ids()`. `create` rejects every representable invalid state: non-positive span, empty tracks/parts, out-of-range ordinals, duplicate parts, any part not exactly tiling `[0, span_length)`, out-of-range pedal spans/clef changes/measure contexts, contradictory duplicate contexts at one coordinate (two clefs for one stave, two signatures at one position), and any id appearing twice. `extract_fragment(const Project&, const Selection&)` is a pure non-mutating query over `FullMeasureSet` and `ArbitraryRangeSet` selections, requiring a diagnostic-free `validate_selection`, one shared node, and one shared measure index or identical span. **Identity invariant:** no id anywhere in a fragment exists in the source project — every event, `ChordNote`, `GraceNote`, and marking id is regenerated, with a per-part source→first-piece map rewriting marking references, so pasting one fragment twice cannot duplicate a UUID. Twelve locked clipping rules R1–R12 govern the boundary: head-straddling sounding events become normalized rests (no invented attack — Adam's ruling), tail-straddling events truncate into a tied continuation chain with the outgoing tie severed, straddling rests re-decompose, tuplets copy only when wholly contained and otherwise fail deterministically with `kInvalidArgument` (Adam's ruling, kind-agnostic across note/chord/rest), slurs and hairpins clip to surviving endpoints, beam overrides filter and drop below two survivors, dynamics and grace groups follow their anchor event, pedal spans intersect, clef changes carry relative with the origin clef recorded separately, and measure structure is carried as informational context only (a pickdown origin inherits the final main-region measure's signatures). Source voices that are empty or shorter than the span are rest-filled to the span rather than rejected — a stave normally holds four voices of which only one is written. 1400 tests (`NotationFragmentTest` 65); debug + ASan/UBSan, all seven audits, and clang-tidy 18 clean. **Paste-side reconnection and cut followed in 8g-ii; node copy/paste remains in 8g-iii.**
- [x] **Phase 8g-ii — reversible cut and paste commands (done, committed, reviewer-approved).** Delivered `PasteFragmentCommand` and `CutFragmentCommand`: complete-measure and non-aligned full-span replacement, exact undo/redo, fresh identity remapping, deterministic directional clipping and tie severing, marking/pedal handling, sparse referenced-coordinate mapping, empty/no-stave support, clef-independent pitch invariance, no-throw allocation/failure atomicity, and multi-lane lifecycle/stale atomicity. 78 `ClipboardCommand` tests; 1481 tests total. Interior clef and signature application remains deferred to 8h.
- [ ] Reversible commands for every graph, notation, tempo, track, and metadata edit.
- [ ] Transaction grouping for multi-measure operations and drag gestures.
- [ ] Stable notehead, chord, full-measure, arbitrary musical-time range, node, connector, and insertion-caret selection, with explicit staff/track/voice scope. **(Phase 8f-i and 8f-ii complete. This deliverable remains unchecked solely because `route-segment` selection is DEFERRED to M06 — geometry only becomes well-defined once the canvas computes drawn paths.)** A `Selection` is a `std::variant` of non-empty homogeneous SETS OF SCOPED ITEMS — each variant arm carries per-item scope (not one shared scope plus payload), supporting aligned multi-track/stave/voice and multi-node selections without Cartesian ambiguity. `route-segment` selection is DEFERRED to M06 (geometry only becomes well-defined once the canvas computes drawn paths). `staff-focus` is DROPPED as a selection kind (context for another operation, not an end state). 8f-i ChordNote + GraceNote identity and 8f-ii selection representation are both complete (seven homogeneous deduplicated set arms, separate deterministic validator, `position_of_event`, `total_length`, 1335 tests). M03 ChordNote/GraceNote identity round-trip obligation carries forward.**
- [x] Toolkit-independent notation clipboard fragments preserve relative musical positions, staff/voice mapping, notation entities, and source duration without retaining source entity UUIDs. **(Phase 8g-i. `NotationFragment` stores every position relative to the fragment origin, maps staff/voice by ordinal rather than by id, carries the full notation entity set including markings and grace groups, records the source span as `span_length`, and provably retains no source UUID — verified by probe, not only by test.)**
- [x] Reversible cut/copy/paste commands support complete measures and non-measure-aligned fragments, remap identities, normalize destination rests, and modify no music outside the destination range.
- [ ] Specify deterministic clipping/reconnection rules for notes, ties, slurs, tuplets, hairpins, pedal spans, dynamics, clef/key/time changes, and other entities crossing a copied range boundary. **(Phases 8g-i and 8g-ii complete the copy/paste-side clipping and reconnection rules. This box stays unchecked because applying copied interior clef/key/time changes requires the `MeasureMap`/`ClefLane` mutation deferred to 8h.)**
- [x] **Phase 8g-iii — reversible node duplication with full identity remapping (done, committed, reviewer-approved).** `DuplicateNodesCommand` duplicates every node of a `NodeSet` selection in place within one `Project`. **Scope decision (Adam):** a direct duplication command, not a `NodeFragment` value type plus `extract`/paste pair — the clipboard shape was considered and explicitly rejected as unnecessary for the M02 deliverable; cross-project node clipboard paste, if ever wanted, belongs to M06. **Identity:** each duplicate gets a fresh `NodeId`, a fresh `ConnectorId` for every input and output, and a fresh `NotationEntityId` for every top-level `VoiceEvent`, embedded `ChordNote`, `GraceNote`, and marking (dynamics, hairpins, slurs, beam overrides, grace groups, stave-scoped pedal spans) in every duplicated `TrackLane` — reusing 8g-i's `regenerate_events`/`add_regenerated_markings`/`make_pedal_span` rather than reimplementing them, with an unremappable marking reference a hard `kInvalidArgument` rather than a silent drop. A global uniqueness probe over active **and archived** track lanes proves no UUID appears twice anywhere in the project. `NodeTimeline` (`MeasureMap`, `ClefLane`s, `TempoLane`) is provably id-free and is copied verbatim; lane `TrackId`/`StaveId` keys are preserved because tracks and staves are project-owned and never duplicated. **Edges:** an edge with both endpoints in the selection is remapped to the duplicate endpoints, including self-loops (the duplicate's output targets the *duplicate's* own input); an edge leaving the selection in either direction is dropped, and no non-selected node is mutated at all. A remapped edge's route is automatic — the duplicate's output is freshly minted with an automatic `RouteGeometry` and the source's is deliberately never copied. `EventId` is project-scoped, so bindings are preserved rather than remapped, with the duplicate's `EventListener` policy and capacity matching the source. Duplicates never become the start node; name/color/notes copy verbatim and position is source-plus-caller-offset (default zero), because layout is the canvas's decision, not the domain's. **Reversibility:** ids are minted exactly once on the first successful `execute` and stored as full `Node` snapshots, so `redo` restores them id-for-id — verified for `NodeId`, `ConnectorId`, and `NotationEntityId`. `Project::remove_node` cascades into *other* nodes' destinations and routes, so a failed multi-node undo repairs cascade damage by removing every still-present duplicate and restoring all of them verbatim from snapshot, latching `kFaulted` only when that repair is provably incomplete; terminal state now surfaces as `kCommandFaulted`, distinguishing "called out of order" from "unrecoverable". 25 `DuplicateNodesCommandTest` cases, 1506 tests total; debug, lint, all seven audits, clang-tidy 18, and ASan/UBSan clean. **Advisory carried forward:** the stale-context pre-check (undo after a duplicate is removed behind the command's back) is reachable without fault injection but has no test yet.
- [ ] Measure insert/delete operations update every track, signature lane, tempo anchors, spans, and compatibility diagnostics atomically. **(Phase 8h; per Adam's post-8e-iii ruling this phase also owns the node-timeline edit commands — per-measure time/key-signature, clef, and pickdown set/clear — that the "reversible commands for every ... notation ... edit" box above depends on.)**
  - **Phase 8h split (Adam's decision).** Four increments, in dependency order. The seam is that any edit changing a measure's *length* — insert, delete, or a time-signature change — must cascade into the tempo lane, the pickdown duration, and every track lane's voice normalization; a *key*-signature change does not. `SetMeasureTimeSignatureCommand` therefore sits with the cascade in 8h-iii, not with the other signature commands in 8h-ii.
    - [x] **8h-i — `MeasureMap` and `ClefLane` mutation primitives (done, committed, reviewer-approved).** The containers had none: `MeasureMap` was construct-only via `create`, and `ClefLane` had only `add_change`. Adds `MeasureMap::insert_measure` (two overloads — explicit `Measure`, and an inheriting overload copying the measure currently at `index` or the last measure when appending; Adam chose both), `remove_measure`, `set_measure`; and `ClefLane::remove_change`, `move_change`, `set_change`. Every mutator rebuilds `starts_`/`total_length_` through the same `compute_derived_state` the constructor uses — never patched incrementally — so derived state is provably identical to a fresh `create` on the same vector, and every mutator is atomic (build-then-commit, no partial mutation on failure). **`remove_measure` refuses to remove the last remaining measure**, discharging the container half of the "at least one complete main-region measure" invariant that `pickdown_coordinates.hpp` warned a future measure-mutator phase would have to supply itself; two live call sites (`pickdown_coordinate_at_position`'s helper and `NodeTimeline::set_pickdown`) compute `measure_count() - 1` on an unsigned `std::size_t` and would underflow without it. Deliberately **no** ids on `Measure`/`ClefChange` (node duplication copies timelines verbatim) and **no** mutable `MeasureMap` accessor on `NodeTimeline` — exposing one would let callers bypass the 8h-iii cascade. 23 new cases, 1531 tests total.
    - [x] **8h-ii — non-length-changing node-timeline commands (done, reviewer-approved).** Added narrow `NodeTimeline` entry points for per-measure key-signature and clef add/remove/move edits plus checked whole-`ClefLane` restoration, without exposing a mutable `MeasureMap`. Six stable-ID, reversible, `noexcept` commands (`SetMeasureKeySignatureCommand`, `AddClefChangeCommand`, `RemoveClefChangeCommand`, `MoveClefChangeCommand`, `SetPickdownCommand`, `ClearPickdownCommand`) use whole-`Measure`/`ClefLane` and exact optional duration/`TempoLane` snapshots, including tempo-lane presence, points, start, and end. They cover the full stale lifecycle and retry/failure atomicity; `restore_clef_lane` enforces default-clef ownership, key edits preserve time signature and timeline length, and pickdown commands retain `NodeTimeline`'s existing set/clear semantics and tempo revalidation. 21 new cases, 1552 tests total.
    - [x] **8h-iii — measure insert/delete plus time-signature cascade (done, reviewer-approved).** Delivered reversible `InsertMeasureCommand` (explicit-`Measure` and inherited-signature forms), `DeleteMeasureCommand`, and `SetMeasureTimeSignatureCommand`. Each builds and validates atomic whole-`Node` before/after candidate snapshots, cascading through every active and archived track lane, every stave's four voices, the measure/clef/tempo timeline, pedal spans, and exact pickdown revalidation. Insert rest-fills the new measure and shifts later material; delete closes the gap; boundary attacks clip with ties severed and no invented attacks. Stable-id pedal endpoints transform with the edit; tempo/clef content inside a deleted measure is removed while later content shifts to the new boundary, including the mandatory origin tempo. Tuplets are indivisible (boundary cuts reject atomically; complete-group boundaries succeed), and beam overrides split by chronological adjacency with the earliest subgroup retaining its id and deterministic later-subgroup ids stable through undo/redo. `TimeSignature::kMaxDenominator` is now 64, matching notation/rest representability. Removal delegates to `MeasureMap`'s sole-measure guard; raw selection indexes are revalidated rather than mutated, and vertical compatibility remains a live query. Representative 64-active-track × 64-measure coverage passes. Reviewer-approved exact-tree evidence: 1599/1599 debug tests, lint over 326 files, all seven architecture audits, canonical clang-tidy 18, and ASan/UBSan 1599/1599; TSan N/A.
    - [ ] **8h-iv — paste applies copied interior clef and signature context.** Closes the clipping/reconnection box below. `NotationFragment` already carries validated, sorted `clef_changes()`, `stave_contexts()`, and `measure_contexts()` that `PasteFragmentCommand` never reads. **Open question for Adam:** applying a copied *time* signature changes measure length and therefore `node_end()`, which collides with paste's currently-locked "paste never grows the node" rule.

### Validation service

- [ ] Fast incremental validation for editor feedback and complete validation for save/export.
- [ ] Diagnostics carry stable entity IDs, severity, machine-readable code, and user-facing text.
- [ ] Validate rhythmic completeness, UUID uniqueness, references, track alignment, signature legality, graph edge integrity, event-name uniqueness, and connector cardinality.

## Acceptance Criteria

- [ ] Domain targets have no UI, renderer, audio-device, plugin, or platform dependencies.
- [ ] All edits required by the `0.1.0` notation/graph scope are expressible as commands and undo/redo exactly.
- [ ] Replaying the same command stream produces structurally equal projects with stable intended IDs.
- [ ] Invalid cross-entity references cannot enter normal model APIs without a diagnostic result.
- [ ] The event, transition, pickdown, tempo, articulation, grace, and MIDI-ownership state machines are complete enough to implement independently in writer/runtime and compare with shared golden vectors.
- [ ] Representative 64-track, 64-measure nodes remain practical to construct, validate, copy, and mutate in tests.
- [ ] Archived track data survives removal, save-model round trip, undo, and restoration.

## Test Focus

- [ ] Value-type boundary and invalid-input tests.
- [ ] Table-driven duration, tuplet, accidental, key, clef, and meter tests.
- [ ] Four-voice rhythm normalization and automatic-rest tests.
- [ ] Command execute/undo/redo round trips, including grouped failure rollback.
- [ ] Deterministic generated edit sequences checked against model invariants.
- [ ] UUID remapping, stale selection, span repair, and cross-measure mutation tests.
- [ ] Whole-measure and arbitrary-range clipboard round trips across voices/staves, including boundary-crossing notation and paste into occupied destination ranges.
- [ ] Incremental diagnostics compared with complete validation results.
- [ ] Event state-machine, same-sample vertical-cycle prevention, pickdown-bound proof-oracle, MIDI ownership, and lifecycle clearing tests.
