// SPDX-License-Identifier: Apache-2.0
//
// Source for the negative consumer probe. Never expected to build: the
// project that owns it is expected to fail at configure time because
// graphscore::writer_app is not exported by the runtime package.

int main() {
  return 0;
}
